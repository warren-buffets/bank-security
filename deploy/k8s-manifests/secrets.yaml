apiVersion: v1
kind: Secret
metadata:
  name: fraudguard-secrets
  namespace: fraudguard
  labels:
    app.kubernetes.io/name: fraudguard
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production!
  # Use kubectl create secret or external secrets operator
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  POSTGRES_HOST: "postgres.fraudguard.svc.cluster.local"
  REDIS_HOST: "redis.fraudguard.svc.cluster.local"
  KAFKA_BOOTSTRAP_SERVERS: "kafka.fraudguard.svc.cluster.local:9092"
