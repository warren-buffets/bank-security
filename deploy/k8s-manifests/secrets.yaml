apiVersion: v1
kind: Secret
metadata:
  name: safeguard-secrets
  namespace: safeguard
  labels:
    app.kubernetes.io/name: safeguard
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production!
  # Use kubectl create secret or external secrets operator
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "CHANGE_ME_IN_PRODUCTION"
  POSTGRES_HOST: "postgres.safeguard.svc.cluster.local"
  REDIS_HOST: "redis.safeguard.svc.cluster.local"
  KAFKA_BOOTSTRAP_SERVERS: "kafka.safeguard.svc.cluster.local:9092"
